//Affichage des oeuvres sur la page d'accueil

    // Importation de la fonction getAndPrintWorks déclarée dans le fichier FrontEnd/assets/scripts/components/gallery.js
        import { getAndPrintWorks } from './components/gallery.js';

    // Recupération et affichage des oeuvres avec l'appel de la fonction getAndPrintWorks
        getAndPrintWorks();
        

// Création des filtres de catégories
    /* 
    Suggestion de l'IA :

    // Importation de la fonction getCategories déclarée dans le fichier FrontEnd/assets/scripts/components/categories.js
        import { getCategories } from './components/categories.js';

    // Recupération et affichage des catégories avec l'appel de la fonction getCategories
        getCategories();
    */